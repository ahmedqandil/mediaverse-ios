"""Mediaverse-owned face-retouching training package.

Submodules are deliberately loaded explicitly so manifest rights validation remains
available in environments where the optional ML dependencies are not installed.
"""
